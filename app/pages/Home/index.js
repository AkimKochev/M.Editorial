import gsap from 'gsap';

export default class Home {
	constructor() {
		this.clipTitle();
	}
	clipTitle() {
		// const titleWrapper = document.querySelector('.home__article__title__wrapper');
		// console.log(titleWrapper);
		// const titles = [...titleWrapper.querySelectorAll('h2')];
		// const height = titles.map((t) => t.getBoundingClientRect().height);
		// console.log(height);
		// titleWrapper.style.height = height[0] * 2 - height[1];
		// titleWrapper.style.overflow = 'hidden';
	}
}
